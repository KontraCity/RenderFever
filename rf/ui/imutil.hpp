#pragma once

namespace rf {

namespace Ui {
    namespace ImUtil {
        // Align: left = 0.0f, center = 0.5f, right = 1.0f.
        void TextUnformattedShiftedRight(const char* text, float ratio);
    }
}

} // namespace rf

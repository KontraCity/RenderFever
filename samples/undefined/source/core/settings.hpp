#pragma once

namespace Undefined {

namespace Settings {
    namespace MovementSpeed {
        constexpr float Normal = 2.5f;
        constexpr float Slow = 1.0f;
        constexpr float Quick = 20.0f;
    }

    namespace Sensitivity {
        constexpr float Look = 0.1f;
        constexpr float Zoom = 0.1f;
    }

    namespace CameraZoom {
        constexpr float Min = 0.3f;
        constexpr float Max = 20.0f;
    }
}

} // namespace Undefined
